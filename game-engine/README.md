# Game Engine Service

Manages Tic Tac Toe board state, validates moves, and reports win/draw outcomes.

## Run

```bash
mvn -f game-engine/pom.xml spring-boot:run
```

The service runs on `http://localhost:8081`.
